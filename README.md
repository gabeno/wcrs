## `wcrs`

This is a cli tool for counting number of words and lines in a file.

### usage

```
Count lines or words in the specified files

Usage: wcrs [OPTIONS] <FILES>...

Arguments:
  <FILES>...  Files to run count on

Options:
  -w, --words  Count words instead of lines
  -h, --help   Print help
```
